export async function sendWelcomeEmail(userEmail, userName) {
  // Frontend-only placeholder.
  // GitHub Pages and localStorage cannot securely send real email by themselves.
  // Connect this later to EmailJS, Resend, Firebase Auth email, or a backend API.
  return {
    prepared: true,
    userEmail,
    userName,
  };
}
